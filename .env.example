PORT=3000
# Set this to a persistent mounted directory in production.
# DATA_DIR=/var/data/afc

# Required for GET /api/bookings, /api/sos, and /api/membership.
ADMIN_TOKEN=replace-with-a-long-random-secret

# Optional. Only configure after a real payment endpoint exists.
# PAYMENT_BASE_URL=https://payments.example.com
